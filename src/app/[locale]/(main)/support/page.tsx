import { SupportClient } from './SupportClient';

export const metadata = {
  title: 'Support — Kole Tex',
  description: 'Welcome to Kole Tex Support. We are here to help.',
};

export default function SupportPage() {
  return <SupportClient />;
}
