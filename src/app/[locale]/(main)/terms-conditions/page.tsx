import { TermsClient } from "./TermsClient";

export const metadata = {
  title: 'Terms & Conditions — Kole Tex',
  description: 'Terms and Conditions for using Kole Tex services.',
};

export default function TermsPage() {
  return <TermsClient />;
}
