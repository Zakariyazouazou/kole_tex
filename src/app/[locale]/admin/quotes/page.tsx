import { QuotesAdminClient } from './QuotesAdminClient';

export const metadata = { title: 'Admin — Quote Management' };

export default function AdminQuotesPage() {
  return <QuotesAdminClient />;
}
